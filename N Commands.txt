arp -a        → Shows IP & MAC address mapping
hostname     → Shows computer name
ipconfig     → Shows basic IP details
ipconfig /all→ Shows full network details
ipconfig /renew → Gets new IP from DHCP
ipconfig /release → Releases IP address
ipconfig /flushdns → Clears DNS cache
nbtstat -a   → NetBIOS information
net diag     → Network diagnostics
netstat      → Active network connections
nslookup     → DNS checking tool